
先new repository
echo "# dairy" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/obugit/dairy.git
git push -u origin master

git remote add origin https://github.com/obugit/dairy.git
git branch -M master
git push -u origin master


//小米电脑push
git clone https://github.com/obugit/dairy.git
cd dairy
git checkout -b xiaomi-laptop
然后编辑文件然后修改然后add commit
git push -u origin xiaomi-laptop //会提示用户名跟密码
git checkout master

//pull远程代码
git pull <远程主机名> <远程分支名>:<本地分支名>
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


在git中，可利用checkout命令转换分支，该命令的作用就是切换分支或恢复工作树文件，语法为“git checkout 分支名”；当参数设置为“-b”时，可以在新分支创建的同时切换分支，语法为“git checkout -b 分支名”

git branch --delete dev
git branch -d dev

##############################################################
后面直接再x250上修改吧，这样子会方便一些

